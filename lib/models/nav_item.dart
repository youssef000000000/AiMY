/// Represents a top navigation pill item (e.g. AiMY Sales, AiMY Widgets).
class NavItem {
  const NavItem({
    required this.id,
    required this.label,
  });

  final String id;
  final String label;
}
